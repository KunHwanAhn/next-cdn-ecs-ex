export type Todo = {
  userId: number;
  id: number;
  title: string;
  completed: boolean
};

export type Album = {
  userId: number;
  id: number;
  title: string;
};

export type Favorite = number;
